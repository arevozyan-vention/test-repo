apiVersion: v1
kind: ConfigMap
metadata:
  name: bookinfo-db
  labels:
    {{- include "bookinfo.labels" . | nindent 4 }}
data:
  DB_TYPE: mysql
  MYSQL_DB_HOST: {{ .Values.database.host }}
  MYSQL_DB_PORT: {{ .Values.database.port | quote }}
  MYSQL_DB_USER: {{ .Values.database.user }}
